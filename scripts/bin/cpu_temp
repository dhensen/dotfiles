#!/bin/zsh

cache=
while true;
do
    cpu_temp=$(sensors -j coretemp-isa-0000 | jq '.[]."Core 0".temp2_input')
    if [[ "$cache" != "$cpu_temp" ]]
    then
        echo -e "R\uf2cb CPU ${cpu_temp}"
        cache=$cpu_temp
    fi
    sleep 1
done
