#!/bin/sh

thisname=$(basename $0)

if [ "$1" == "" ]; then
    echo "usage: $thisname <video_extension>"
    echo "example: $thisname mp4"
    exit 1
fi

for f in *."$1"; do downloadsubtitle "$f" eng; done
