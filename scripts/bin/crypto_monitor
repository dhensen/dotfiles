#!/usr/bin/env bash

while true;
do
    price=$(curl -s https://blockchain.info/ticker | jq -j '.EUR | "\(.symbol) \(.last)"')
    time=$(date +"%H:%M:%S")
    to_fifo "Q BTC: ${price} ($time)"
    sleep 10
done
