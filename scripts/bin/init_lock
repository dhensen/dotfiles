#!/bin/bash
set -x

# notify lock after 4 minutes (240 seconds)
# actually lock 1 minute after that
xset s 240 60

pkill xss-lock
exec xss-lock \
    --notifier lock_notify \
    --transfer-sleep-lock \
    --verbose \
    -- \
    transfer-sleep-lock.sh
